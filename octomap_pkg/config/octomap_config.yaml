octomap_server:
  ros__parameters:
    frame_id: "world"

    point_cloud_topic: "/camera/depth/color/points"

    resolution: 0.005

    sensor_model:
      max_range: 1.0
      min_range: 0.15

    occupied_thresh: 0.65

    free_thresh: 0.30

    latch: false
